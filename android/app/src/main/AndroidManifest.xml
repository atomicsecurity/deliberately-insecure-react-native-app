<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- DIRNA-VULN:m8_backup_cleartext — allowBackup=true lets `adb backup` exfiltrate app-private data; a cleartext-permitting network security config is wired in too. INTENTIONALLY INSECURE. -->
    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="true"
      android:networkSecurityConfig="@xml/network_security_config"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <!-- DIRNA-VULN:m8_exported_deeplink — MainActivity is exported with browsable deep-link filters, so any app/website can launch it with attacker-chosen data. INTENTIONALLY INSECURE. -->
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- DIRNA-VULN:m4_deeplink_sink — custom `dirna://open` deep link; the routed URL is passed to a sink with no validation. INTENTIONALLY INSECURE. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="dirna" android:host="open" />
        </intent-filter>
        <!-- DIRNA-VULN:m4_deeplink_sink — plaintext `http://` deep-link surface: any http link can drive the app. INTENTIONALLY INSECURE. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="http" />
        </intent-filter>
      </activity>
    </application>
</manifest>
